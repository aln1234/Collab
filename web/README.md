# Connect Web

Mobile-first Next.js web app/PWA shell for the Connect creator marketplace MVP.

## Commands

```bash
npm run dev
npm run lint
npm run build
```

Set `NEXT_PUBLIC_API_URL` in `.env.local` when the backend is not running at `http://localhost:8000/api`.
